{
  "name": "simple-codex-telegram-bridge",
  "version": "0.1.0",
  "description": "A minimal Telegram bridge for Codex — simple setup, full approval control",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.110.0",
    "dotenv": "^16.0.0",
    "grammy": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "codex-tg": "dist/index.js"
  }
}
